2026-05-11

--> Review JavaScript Fundamentals

    - Variables
        - strings, booleans, numbers
        - naming conventions
        - empty varaibles (undefined)
        - outputting variables (literals)
        - constants
    - Operators
        - add, subtract, multiply, divide, modulus
        - Math library (Math.x)
        - arithmetic shortcuts (+=, -=, /=, *=)
        - comparison operators (>, <, >=, <=)
        - loose equality (==); value match only; performs type coercion on second value
        - strict equality (===); value and type match
        - logical operators (&&, ||, !)
        - implicit type coercion (ex. 47 + "5" = "475"; "5" - 2 = 3)
        - explicit type coercion (ex. String(num))
    - if & switch statements
        - if, else if, else
        - nested if statements
        - switch statements (syntax)
    - Basic I/O
        - Type conversion (casting) (ex. parseFloat/parseInt, Boolean(value), String(value))
    - Loops
        - for loops
        - while loops
        - do, while loops
        - break (exit)
        - continue (skip for iteration)
        - nested loops
    - Functions
        - declaring a function
        - arguments/params
        - return statements
        - function expressions (store function in a variable)
        - function declarations (delcare function only)
        - default parameters
        - arrow functions (syntax)
            - 1 parameter syntax
            - 1 line return syntax
            - default syntax
    - Arrays
        - declaring an array
        - looping through arrays and printing indexes (for loop, for...of loop [+strings])
        - modifying arrays (arr[idx] = value)
        - swapping elements (temp variables)
        - methods: sort(), reverse(), indexOf(element), lastIndexOf(element), includes(element)
            - returning -1 (indexOf() and lastIndexOf())
        - more methods: forEach(), slice(start, stop) [inclusive/exclusive]
        - more methods: concat(), join(delimiter), slice(start, end), splice(start, deleteCount, item1, item2, ...)
        - more methods: map(), filter(), reduce()

2026-05-12

--> Review JavaScript Fundamentals (2)
    
    - String Methods
        - String methods (share methods with arrays)
        - includes(), indexOf(), length, and slice()
        - don't have modification methods like push() and pop()
        - string are immutable, if you do str[1] = 'a', it will still output the original string

    - String methods continued
        - toUpperCase(), toLowerCase(), replace(), replaceAll(), trim(), charAt()

    - Multi-dimensional arrays
        - syntax: matrix[row][column]
        - looping through 2D arrays
        - adding and subtracting 2D arrays
        - 3D arrays (each element is a 2D array)

    - JSON (JavaScript Object Notation)
        - creating an object (key-values)
        - accessing/updating values 
            - bracket Notation
            - dot Notation
        - check if key exists
            -  if(<value_name> in <object_name>)
            - <object_name>.hasOwnProperty(<value_name>)
        - Object methods
            - Object.keys(obj): returns array of keys
            - Object.values(obj) :returns array of values
            - Object.entries(obj): returns an array of [key, value] pairs